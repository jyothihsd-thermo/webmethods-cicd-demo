name: Thermofisher_CICD_Devops_NonPROD_script

on:
  workflow_dispatch:
    inputs:

      action:
        description: "Select the CI/CD action to execute"
        required: true
        type: choice
        options:
          - Build
          - Build and Deploy
          - Deploy

      buildToDeploy:
        description: "Existing ABE build to deploy, e.g. build.10.15.131"
        required: false
        type: string

      targetEnvironment:
        description: "Target environment"
        required: true
        type: string

      configDeployment:
        description: "Configuration deployment scope"
        required: true
        type: choice
        options:
          - NONE
          - ALL
          - SELECTIVE
        default: NONE

      configFiles:
        description: "Configuration file(s) for SELECTIVE deployment, comma-separated"
        required: false
        type: string

      deploymentScope:
        description: "Deployment scope"
        required: true
        type: choice
        options:
          - ALL
          - SELECTIVE
        default: ALL

      packageName:
        description: "Package name(s) for SELECTIVE deployment, comma-separated"
        required: false
        type: string

      projectName:
        description: "ABE/CI-CD project name"
        required: true
        type: string

      targetISGroupAlias:
        description: "Target IS Group Alias"
        required: true
        type: choice
        options:
          - RepoISTarget
          - TestISTarget
        default: RepoISTarget

      buildNumber:
        description: "Build number (required for Build and Build and Deploy)"
        required: false
        type: string

      hasAdapterPackage:
        description: "Does deployment contain an Adapter package?"
        required: true
        type: boolean
        default: false

      adapterConfigPackage:
        description: "Adapter configuration package"
        required: false
        type: string

      RepoBranchName:
        description: "Repository branch"
        required: true
        type: choice
        options:
          - main
          - develop
          - release
        default: main

      executeWmTestSuite:
        description: "Execute webMethods Test Suite"
        required: true
        type: boolean
        default: false


jobs:

  Thermofisher_CICD_Devops_NonPROD_script:

    runs-on:
      - self-hosted
      - windows
      - webmethods

    # Prevent concurrent deployments to the same environment from
    # overwriting each other's rollback state.
    concurrency:
      group: webmethods-${{ inputs.targetEnvironment }}
      cancel-in-progress: false

    env:

      # ============================================================
      # CI/CD Installation / Workspace Configuration
      # ============================================================

      CICD_HOME: C:/ibm-devops/cicd

      SAG_BUILD_HOME: C:/SoftwareAG11_ABE

      SAG_HOME: C:/SoftwareAG11_ABE

      ANT_HOME: C:/SoftwareAG11_ABE/common/lib/ant

      JAVA_HOME: C:/SoftwareAG11/jvm/jvm/bin

      CHECKOUT_DIR: C:/ibm-devops/cicd/process/GitCheckout/Checkout

      SELECTIVE_SOURCE_DIR: C:/ibm-devops/cicd/process/GitCheckout/CheckoutSelective

      CONFIG_SELECTIVE_SOURCE_DIR: C:/ibm-devops/cicd/process/GitCheckout/SelectiveConfig
      CONFIG_EMPTY_SOURCE_DIR: C:/ibm-devops/cicd/process/GitCheckout/ConfigEmpty
      ABE_SOURCE_DIR: C:/ibm-devops/cicd/process/Build/ABE_Source
      BUILD_OUTPUT_DIR: C:/ibm-devops/cicd/process/Build/buildDeployOutput/output1/${{ inputs.projectName }}

      BUILD_BACKUP_DIR: C:/ibm-devops/cicd/process/Build/buildsBackup

      # Persistent deployment state used for automatic rollback.
      DEPLOYMENT_STATE_DIR: C:/ibm-devops/cicd/process/Build/buildsBackup/deployment-state


      # ============================================================
      # GitHub Actions Inputs
      # ============================================================

      ACTION: ${{ inputs.action }}
      BUILD_TO_DEPLOY: ${{ inputs.buildToDeploy }}

      TARGET_ENVIRONMENT: ${{ inputs.targetEnvironment }}

      CONFIG_DEPLOYMENT: ${{ inputs.configDeployment }}
      CONFIG_FILES: ${{ inputs.configFiles }}

      DEPLOYMENT_SCOPE: ${{ inputs.deploymentScope }}

      ISPACKAGES_LIST: ${{ inputs.packageName }}

      PROJECT_NAME: ${{ inputs.projectName }}

      TARGET_ISGROUP_ALIAS: ${{ inputs.targetISGroupAlias }}

      MAIN_BUILDNUMBER: ${{ inputs.buildNumber }}

      HAS_ADAPTERPKG: ${{ inputs.hasAdapterPackage }}

      ADAPTER_CONFIGPKG: ${{ inputs.adapterConfigPackage }}

      EXECUTE_WMTEST: ${{ inputs.executeWmTestSuite }}


    steps:

      # ============================================================
      # 1. Checkout Repository
      # ============================================================

      - name: Checkout Repository
        if: ${{ inputs.action != 'Deploy' }}
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.RepoBranchName }}
          clean: true


      # ============================================================
      # 2. Validate Required Inputs
      # ============================================================

      - name: Validate Required Inputs
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Validating workflow inputs"
          Write-Host "=================================================="

          Write-Host "Action                : [$env:ACTION]"
          Write-Host "Build To Deploy       : [$env:BUILD_TO_DEPLOY]"
          Write-Host "Target Environment    : [$env:TARGET_ENVIRONMENT]"
          Write-Host "Config Deployment     : [$env:CONFIG_DEPLOYMENT]"
          Write-Host "Config Files           : [$env:CONFIG_FILES]"
          Write-Host "Deployment Scope      : [$env:DEPLOYMENT_SCOPE]"
          Write-Host "Package List          : [$env:ISPACKAGES_LIST]"
          Write-Host "Project Name          : [$env:PROJECT_NAME]"
          Write-Host "Target IS Group Alias : [$env:TARGET_ISGROUP_ALIAS]"
          Write-Host "Build Number          : [$env:MAIN_BUILDNUMBER]"
          Write-Host "Has Adapter Package   : [$env:HAS_ADAPTERPKG]"
          Write-Host "Adapter Config        : [$env:ADAPTER_CONFIGPKG]"
          Write-Host "Execute WM Test       : [$env:EXECUTE_WMTEST]"

          if ([string]::IsNullOrWhiteSpace($env:TARGET_ENVIRONMENT)) {
              Write-Error "Target environment is required."
              exit 1
          }

          if ([string]::IsNullOrWhiteSpace($env:PROJECT_NAME)) {
              Write-Error "ABE/CI-CD project name is required."
              exit 1
          }

          $projectName = $env:PROJECT_NAME.Trim()

          if ([string]::IsNullOrWhiteSpace($projectName)) {
              Write-Error "ABE/CI-CD project name cannot be empty."
              exit 1
          }

          "PROJECT_NAME=$projectName" |
              Out-File `
                  $env:GITHUB_ENV `
                  -Encoding utf8 `
                  -Append

          Write-Host "Normalized Project Name : [$projectName]"

          if ($env:ACTION -notin @("Build", "Build and Deploy", "Deploy")) {
              Write-Error "Invalid action: [$env:ACTION]. Allowed values are Build, Build and Deploy, Deploy."
              exit 1
          }

          if ($env:ACTION -in @("Build", "Build and Deploy")) {
              if ([string]::IsNullOrWhiteSpace($env:MAIN_BUILDNUMBER)) {
                  Write-Error "Build number is required for action [$env:ACTION]."
                  exit 1
              }
          }

          if ($env:ACTION -in @("Build and Deploy", "Deploy")) {
              if ([string]::IsNullOrWhiteSpace($env:TARGET_ISGROUP_ALIAS)) {
                  Write-Error "Target IS Group Alias is required for action [$env:ACTION]."
                  exit 1
              }
          }

          if ($env:ACTION -eq "Deploy") {

              if ([string]::IsNullOrWhiteSpace($env:BUILD_TO_DEPLOY)) {
                  Write-Error "buildToDeploy is required when action is Deploy."
                  exit 1
              }

              if ($env:BUILD_TO_DEPLOY -match '[\\/:]') {
                  Write-Error "Invalid build name. Use an ABE build directory name such as build.10.15.131."
                  exit 1
              }

              $buildPath = Join-Path $env:BUILD_BACKUP_DIR $env:BUILD_TO_DEPLOY

              Write-Host "Requested Build       : [$env:BUILD_TO_DEPLOY]"
              Write-Host "Existing Build Path   : [$buildPath]"

              if (-not (Test-Path $buildPath -PathType Container)) {
                  Write-Error "Requested build does not exist: $buildPath"
                  exit 1
              }

              Write-Host "Existing build found successfully."
          }

          if ($env:CONFIG_DEPLOYMENT -notin @("NONE", "ALL", "SELECTIVE")) {
              Write-Error "Invalid configuration deployment option: [$env:CONFIG_DEPLOYMENT]"
              exit 1
          }

          if ($env:CONFIG_DEPLOYMENT -eq "SELECTIVE") {
              if ([string]::IsNullOrWhiteSpace($env:CONFIG_FILES)) {
                  Write-Error "Configuration file(s) are required when configuration deployment is SELECTIVE."
                  exit 1
              }
          }

          if ($env:DEPLOYMENT_SCOPE -eq "SELECTIVE") {

              if ([string]::IsNullOrWhiteSpace($env:ISPACKAGES_LIST)) {
                  Write-Error "Package name(s) are required when deployment scope is SELECTIVE."
                  exit 1
              }
          }

          Write-Host ""
          Write-Host "Required input validation successful."


      # ============================================================
      # 3. Mirror GitHub Workspace to Fixed Checkout Directory
      # ============================================================

      - name: Mirror Repository to Checkout Directory
        if: ${{ inputs.action != 'Deploy' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Mirroring GitHub workspace"
          Write-Host "=================================================="

          Write-Host "Source      : [$env:GITHUB_WORKSPACE]"
          Write-Host "Destination : [$env:CHECKOUT_DIR]"

          if (-not (Test-Path $env:CHECKOUT_DIR)) {
              New-Item `
                  -ItemType Directory `
                  -Path $env:CHECKOUT_DIR `
                  -Force |
                  Out-Null
          }

          robocopy `
              "$env:GITHUB_WORKSPACE" `
              "$env:CHECKOUT_DIR" `
              /MIR `
              /XD ".git"

          $robocopyExitCode = $LASTEXITCODE

          Write-Host "Robocopy exit code: $robocopyExitCode"

          if ($robocopyExitCode -ge 8) {
              Write-Error `
                  "Repository mirror failed. Robocopy exit code: $robocopyExitCode"
              exit 1
          }

          exit 0


      # ============================================================
      # 4. Prepare Build Source
      #
      # ALL:
      #   C:/ibm-devops/cicd/process/GitCheckout/Checkout/is/packages
      #
      # SELECTIVE:
      #   C:/ibm-devops/cicd/process/GitCheckout/CheckoutSelective
      #
      # Robocopy exit codes:
      #   0-7 = success
      #   8+  = failure
      # ============================================================

      - name: Prepare Build Source
        if: ${{ inputs.action != 'Deploy' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Preparing build source"
          Write-Host "=================================================="

          Write-Host "Deployment Scope : [$env:DEPLOYMENT_SCOPE]"
          Write-Host "Package List     : [$env:ISPACKAGES_LIST]"

          # --------------------------------------------------------
          # ALL PACKAGE DEPLOYMENT
          # --------------------------------------------------------

          if ($env:DEPLOYMENT_SCOPE -eq "ALL") {

              $buildSourceDir = "$env:CHECKOUT_DIR/is/packages"

              Write-Host ""
              Write-Host "ALL deployment selected."
              Write-Host "Build source directory:"
              Write-Host "[$buildSourceDir]"

              if (-not (Test-Path $buildSourceDir)) {

                  Write-Error `
                      "Build source directory does not exist: $buildSourceDir"

                  exit 1
              }
          }

          # --------------------------------------------------------
          # SELECTIVE PACKAGE DEPLOYMENT
          # --------------------------------------------------------

          elseif ($env:DEPLOYMENT_SCOPE -eq "SELECTIVE") {

              Write-Host ""
              Write-Host "SELECTIVE deployment selected."

              $buildSourceDir = $env:SELECTIVE_SOURCE_DIR

              Write-Host "Selective build source directory:"
              Write-Host "[$buildSourceDir]"

              # Clean previous selective build source

              if (Test-Path $buildSourceDir) {

                  Write-Host ""
                  Write-Host "Cleaning previous selective source directory."

                  Remove-Item `
                      $buildSourceDir `
                      -Recurse `
                      -Force
              }

              New-Item `
                  -ItemType Directory `
                  -Path $buildSourceDir `
                  -Force |
                  Out-Null


              # ----------------------------------------------------
              # Parse comma-separated package names
              # ----------------------------------------------------

              $packages = $env:ISPACKAGES_LIST `
                  -split "," |
                  ForEach-Object {
                      $_.Trim()
                  } |
                  Where-Object {
                      -not [string]::IsNullOrWhiteSpace($_)
                  } |
                  Select-Object -Unique


              if ($packages.Count -eq 0) {

                  Write-Error `
                      "No valid package names were supplied for SELECTIVE deployment."

                  exit 1
              }


              Write-Host ""
              Write-Host "Packages selected for deployment:"

              foreach ($package in $packages) {
                  Write-Host "  - [$package]"
              }


              # ----------------------------------------------------
              # Copy each selected package
              # ----------------------------------------------------

              foreach ($packageName in $packages) {

                  Write-Host ""
                  Write-Host "--------------------------------------------------"
                  Write-Host "Processing package: [$packageName]"
                  Write-Host "--------------------------------------------------"


                  # Prevent path traversal / invalid paths

                  if (
                      $packageName.Contains("..") -or
                      $packageName.Contains("/") -or
                      $packageName.Contains("\") -or
                      $packageName.Contains(":")
                  ) {

                      Write-Error `
                          "Invalid package name: [$packageName]"

                      exit 1
                  }


                  $sourcePackage = Join-Path `
                      "$env:CHECKOUT_DIR/is/packages" `
                      $packageName

                  $targetPackage = Join-Path `
                      $buildSourceDir `
                      $packageName


                  Write-Host "Source package:"
                  Write-Host "[$sourcePackage]"

                  Write-Host "Target package:"
                  Write-Host "[$targetPackage]"


                  # Validate source package

                  if (-not (Test-Path $sourcePackage)) {

                      Write-Error `
                          "Package does not exist: $sourcePackage"

                      exit 1
                  }


                  # Create target package directory

                  New-Item `
                      -ItemType Directory `
                      -Path $targetPackage `
                      -Force |
                      Out-Null


                  # ------------------------------------------------
                  # Robocopy
                  # ------------------------------------------------

                  robocopy `
                      "$sourcePackage" `
                      "$targetPackage" `
                      /E

                  $robocopyExitCode = $LASTEXITCODE

                  Write-Host ""
                  Write-Host "Robocopy exit code: $robocopyExitCode"


                  # Robocopy:
                  # 0-7 = successful completion
                  # 8+  = failure

                  if ($robocopyExitCode -ge 8) {

                      Write-Error `
                          "Failed to copy package [$packageName]. Robocopy exit code: $robocopyExitCode"

                      exit 1
                  }

                  Write-Host `
                      "Package [$packageName] copied successfully."
              }
          }

          else {

              Write-Error `
                  "Invalid deployment scope: [$env:DEPLOYMENT_SCOPE]"

              exit 1
          }


          # ========================================================
          # Persist BUILD_SOURCE_DIR for subsequent workflow steps
          # ========================================================

          Write-Host ""
          Write-Host "=================================================="
          Write-Host "Build source prepared successfully"
          Write-Host "=================================================="

          Write-Host "BUILD_SOURCE_DIR = [$buildSourceDir]"

          "BUILD_SOURCE_DIR=$buildSourceDir" |
              Out-File `
                  $env:GITHUB_ENV `
                  -Encoding utf8 `
                  -Append


          # IMPORTANT:
          # Robocopy may return 1-7 even when the copy succeeded.
          # Explicitly return 0 so GitHub Actions marks this step
          # as successful.

          exit 0


      # ============================================================
      # 5. Prepare Configuration Source
      #
      # NONE:
      #   Use an empty directory so no configuration is included.
      #
      # ALL:
      #   Use all .cnf files under the target environment directory.
      #
      # SELECTIVE:
      #   Copy only the requested .cnf files into a clean directory.
      # ============================================================

      - name: Prepare Configuration Source
        if: ${{ inputs.action != 'Deploy' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Preparing configuration source"
          Write-Host "=================================================="

          Write-Host "Target Environment : [$env:TARGET_ENVIRONMENT]"
          Write-Host "Config Deployment  : [$env:CONFIG_DEPLOYMENT]"
          Write-Host "Config Files       : [$env:CONFIG_FILES]"

          $targetEnvironment = $env:TARGET_ENVIRONMENT.Trim().ToLower()

          $environmentConfigDir = Join-Path `
              "$env:CHECKOUT_DIR/is/config" `
              $targetEnvironment

          Write-Host "Normalized target environment : [$targetEnvironment]"
          Write-Host "Environment config directory : [$environmentConfigDir]"

          # --------------------------------------------------------
          # NONE
          # --------------------------------------------------------

          if ($env:CONFIG_DEPLOYMENT -eq "NONE") {

              Write-Host ""
              Write-Host "Configuration deployment disabled."

              if (Test-Path $env:CONFIG_EMPTY_SOURCE_DIR) {
                  Remove-Item `
                      $env:CONFIG_EMPTY_SOURCE_DIR `
                      -Recurse `
                      -Force
              }

              New-Item `
                  -ItemType Directory `
                  -Path $env:CONFIG_EMPTY_SOURCE_DIR `
                  -Force |
                  Out-Null

              $configSourceDir = $env:CONFIG_EMPTY_SOURCE_DIR
          }

          # --------------------------------------------------------
          # ALL
          # --------------------------------------------------------

          elseif ($env:CONFIG_DEPLOYMENT -eq "ALL") {

              Write-Host ""
              Write-Host "ALL configuration deployment selected."

              if (-not (Test-Path $environmentConfigDir -PathType Container)) {
                  Write-Error `
                      "Environment configuration directory does not exist: $environmentConfigDir"
                  exit 1
              }

              $configFilesFound = Get-ChildItem `
                  -Path $environmentConfigDir `
                  -Filter "*.cnf" `
                  -File

              if ($configFilesFound.Count -eq 0) {
                  Write-Error `
                      "No .cnf configuration files found in: $environmentConfigDir"
                  exit 1
              }

              Write-Host "Configuration files included:"
              foreach ($configFile in $configFilesFound) {
                  Write-Host "  - [$($configFile.Name)]"
              }

              $configSourceDir = $environmentConfigDir
          }

          # --------------------------------------------------------
          # SELECTIVE
          # --------------------------------------------------------

          elseif ($env:CONFIG_DEPLOYMENT -eq "SELECTIVE") {

              Write-Host ""
              Write-Host "SELECTIVE configuration deployment selected."

              if (-not (Test-Path $environmentConfigDir -PathType Container)) {
                  Write-Error `
                      "Environment configuration directory does not exist: $environmentConfigDir"
                  exit 1
              }

              if (Test-Path $env:CONFIG_SELECTIVE_SOURCE_DIR) {
                  Remove-Item `
                      $env:CONFIG_SELECTIVE_SOURCE_DIR `
                      -Recurse `
                      -Force
              }

              New-Item `
                  -ItemType Directory `
                  -Path $env:CONFIG_SELECTIVE_SOURCE_DIR `
                  -Force |
                  Out-Null

              $configFiles = $env:CONFIG_FILES `
                  -split "," |
                  ForEach-Object { $_.Trim() } |
                  Where-Object { -not [string]::IsNullOrWhiteSpace($_) } |
                  Select-Object -Unique

              if ($configFiles.Count -eq 0) {
                  Write-Error "No valid configuration files were supplied."
                  exit 1
              }

              foreach ($configFileName in $configFiles) {

                  Write-Host ""
                  Write-Host "Processing configuration file: [$configFileName]"

                  # Prevent path traversal and sub-directory paths.
                  if (
                      $configFileName.Contains("..") -or
                      $configFileName.Contains("/") -or
                      $configFileName.Contains("\") -or
                      $configFileName.Contains(":")
                  ) {
                      Write-Error `
                          "Invalid configuration file name: [$configFileName]"
                      exit 1
                  }

                  if (-not $configFileName.EndsWith(".cnf")) {
                      Write-Error `
                          "Configuration file must have .cnf extension: [$configFileName]"
                      exit 1
                  }

                  $sourceConfigFile = Join-Path `
                      $environmentConfigDir `
                      $configFileName

                  $targetConfigFile = Join-Path `
                      $env:CONFIG_SELECTIVE_SOURCE_DIR `
                      $configFileName

                  Write-Host "Source : [$sourceConfigFile]"
                  Write-Host "Target : [$targetConfigFile]"

                  if (-not (Test-Path $sourceConfigFile -PathType Leaf)) {
                      Write-Error `
                          "Configuration file does not exist: $sourceConfigFile"
                      exit 1
                  }

                  Copy-Item `
                      $sourceConfigFile `
                      $targetConfigFile `
                      -Force

                  Write-Host "Configuration file copied successfully."
              }

              $configSourceDir = $env:CONFIG_SELECTIVE_SOURCE_DIR
          }

          else {
              Write-Error `
                  "Invalid configuration deployment option: [$env:CONFIG_DEPLOYMENT]"
              exit 1
          }

          Write-Host ""
          Write-Host "=================================================="
          Write-Host "Configuration source prepared successfully"
          Write-Host "=================================================="
          Write-Host "CONFIG_SOURCE_DIR = [$configSourceDir]"

          "CONFIG_SOURCE_DIR=$configSourceDir" |
              Out-File `
                  $env:GITHUB_ENV `
                  -Encoding utf8 `
                  -Append

          exit 0


      # ============================================================
      # 5. Verify Build Source
      # ============================================================

      - name: Verify Build Source
        if: ${{ inputs.action != 'Deploy' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Verifying build source"
          Write-Host "=================================================="

          Write-Host "BUILD_SOURCE_DIR = [$env:BUILD_SOURCE_DIR]"

          if ([string]::IsNullOrWhiteSpace($env:BUILD_SOURCE_DIR)) {

              Write-Error "BUILD_SOURCE_DIR is not set."
              exit 1
          }

          if (-not (Test-Path $env:BUILD_SOURCE_DIR)) {

              Write-Error `
                  "BUILD_SOURCE_DIR does not exist: $env:BUILD_SOURCE_DIR"

              exit 1
          }

          Write-Host ""
          Write-Host "Build source contents:"

          Get-ChildItem `
              -Path $env:BUILD_SOURCE_DIR `
              -Directory |
              Select-Object Name, FullName |
              Format-Table -AutoSize

          exit 0


      # ============================================================
      # 6. Verify Configuration Source
      # ============================================================

      - name: Verify Configuration Source
        if: ${{ inputs.action != 'Deploy' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Verifying configuration source"
          Write-Host "=================================================="

          Write-Host "CONFIG_SOURCE_DIR = [$env:CONFIG_SOURCE_DIR]"

          if ([string]::IsNullOrWhiteSpace($env:CONFIG_SOURCE_DIR)) {
              Write-Error "CONFIG_SOURCE_DIR is not set."
              exit 1
          }

          if (-not (Test-Path $env:CONFIG_SOURCE_DIR -PathType Container)) {
              Write-Error `
                  "CONFIG_SOURCE_DIR does not exist: $env:CONFIG_SOURCE_DIR"
              exit 1
          }

          $configFiles = Get-ChildItem `
              -Path $env:CONFIG_SOURCE_DIR `
              -Filter "*.cnf" `
              -File

          if ($env:CONFIG_DEPLOYMENT -eq "NONE") {
              if ($configFiles.Count -ne 0) {
                  Write-Error `
                      "Configuration deployment is NONE, but .cnf files were found in CONFIG_SOURCE_DIR."
                  exit 1
              }

              Write-Host "No configuration files will be included in the build."
          }
          else {
              if ($configFiles.Count -eq 0) {
                  Write-Error `
                      "No .cnf configuration files found in CONFIG_SOURCE_DIR: $env:CONFIG_SOURCE_DIR"
                  exit 1
              }

              Write-Host "Configuration files to be included:"
              foreach ($configFile in $configFiles) {
                  Write-Host "  - [$($configFile.Name)]"
              }
          }

          exit 0


      # ============================================================
      # 7. Prepare ABE Source Root
      #
      # ABE consumes one source root containing sibling packages
      # and config directories. The Git repository is transformed
      # into that structure here.
      # ============================================================

      - name: Prepare ABE Source Root
        if: ${{ inputs.action != 'Deploy' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Preparing ABE source root"
          Write-Host "=================================================="

          $abeSourceRoot = $env:ABE_SOURCE_DIR
          $abePackagesDir = Join-Path $abeSourceRoot "packages"
          $abeConfigDir = Join-Path $abeSourceRoot "config"

          Write-Host "ABE source root : [$abeSourceRoot]"
          Write-Host "Package source  : [$env:BUILD_SOURCE_DIR]"
          Write-Host "Config source   : [$env:CONFIG_SOURCE_DIR]"

          if (Test-Path $abeSourceRoot) {
              Remove-Item $abeSourceRoot -Recurse -Force
          }

          New-Item -ItemType Directory -Path $abePackagesDir -Force | Out-Null
          New-Item -ItemType Directory -Path $abeConfigDir -Force | Out-Null

          # Copy prepared package source into ABE_Source/packages.
          robocopy `
              "$env:BUILD_SOURCE_DIR" `
              "$abePackagesDir" `
              /E

          $packageCopyExitCode = $LASTEXITCODE
          Write-Host "Package robocopy exit code: [$packageCopyExitCode]"

          if ($packageCopyExitCode -ge 8) {
              Write-Error "Failed to prepare ABE package source. Robocopy exit code: $packageCopyExitCode"
              exit 1
          }

          # Copy the selected configuration source into ABE_Source/config.
          if ($env:CONFIG_DEPLOYMENT -ne "NONE") {

              robocopy `
                  "$env:CONFIG_SOURCE_DIR" `
                  "$abeConfigDir" `
                  /E

              $configCopyExitCode = $LASTEXITCODE
              Write-Host "Config robocopy exit code: [$configCopyExitCode]"

              if ($configCopyExitCode -ge 8) {
                  Write-Error "Failed to prepare ABE configuration source. Robocopy exit code: $configCopyExitCode"
                  exit 1
              }
          }
          else {
              Write-Host "Configuration deployment is NONE. ABE config directory will remain empty."
          }

          Write-Host ""
          Write-Host "ABE source root prepared:"
          Get-ChildItem $abeSourceRoot -Recurse |
              Select-Object FullName, Length |
              Format-Table -AutoSize

          "ABE_SOURCE_DIR=$abeSourceRoot" |
              Out-File $env:GITHUB_ENV -Encoding utf8 -Append

          exit 0


      # ============================================================
      # 8. Verify ABE / Ant Installation
      # ============================================================

      - name: Verify ABE and Ant Installation
        if: ${{ inputs.action != 'Deploy' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Verifying ABE / Ant installation"
          Write-Host "=================================================="

          $antExecutable = "$env:SAG_HOME/common/lib/ant/bin/ant.bat"

          $antBuildFile = `
              "$env:CICD_HOME/scripts/build-and-deploy/run/build.xml"

          $abeBuildProperties = `
              "$env:SAG_HOME/common/AssetBuildEnvironment/master_build/build.properties"


          Write-Host "SAG_HOME             : [$env:SAG_HOME]"
          Write-Host "CICD_HOME            : [$env:CICD_HOME]"
          Write-Host "Ant executable       : [$antExecutable]"
          Write-Host "Ant build file       : [$antBuildFile]"
          Write-Host "ABE build.properties : [$abeBuildProperties]"


          if (-not (Test-Path $antExecutable)) {

              Write-Error `
                  "Ant executable does not exist: $antExecutable"

              exit 1
          }


          if (-not (Test-Path $antBuildFile)) {

              Write-Error `
                  "CI/CD Ant build file does not exist: $antBuildFile"

              exit 1
          }


          if (-not (Test-Path $abeBuildProperties)) {

              Write-Error `
                  "ABE build.properties does not exist: $abeBuildProperties"

              exit 1
          }


          Write-Host ""
          Write-Host "ABE / Ant validation successful."

          exit 0


      # ============================================================
      # 7. Capture rollback state
      # ============================================================

      - name: Capture Previous Successful Deployment
        if: ${{ inputs.action == 'Build and Deploy' || inputs.action == 'Deploy' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Capturing previous successful deployment"
          Write-Host "=================================================="

          $stateDir = $env:DEPLOYMENT_STATE_DIR
          $stateFile = Join-Path $stateDir "$($env:TARGET_ENVIRONMENT).json"

          if (-not (Test-Path $stateDir)) {
              New-Item -ItemType Directory -Path $stateDir -Force | Out-Null
          }

          $rollbackBuild = ""

          if (Test-Path $stateFile -PathType Leaf) {
              try {
                  $state = Get-Content $stateFile -Raw | ConvertFrom-Json
                  $rollbackBuild = [string]$state.lastSuccessfulBuild
              }
              catch {
                  Write-Error "Unable to read deployment state file: $stateFile"
                  Write-Error $_
                  exit 1
              }
          }

          if ([string]::IsNullOrWhiteSpace($rollbackBuild)) {
              Write-Host "No previous successful build is recorded for environment [$env:TARGET_ENVIRONMENT]."
              Write-Host "If the new deployment fails, automatic rollback will not be attempted."
          }
          else {
              $rollbackPath = Join-Path "$env:BUILD_BACKUP_DIR" $rollbackBuild

              Write-Host "Previous successful build : [$rollbackBuild]"
              Write-Host "Previous build path       : [$rollbackPath]"

              if (-not (Test-Path $rollbackPath -PathType Container)) {
                  Write-Error "Recorded rollback build does not exist: $rollbackPath"
                  exit 1
              }
          }

          "ROLLBACK_BUILD=$rollbackBuild" |
              Out-File $env:GITHUB_ENV -Encoding utf8 -Append

          "DEPLOYMENT_STATE_FILE=$stateFile" |
              Out-File $env:GITHUB_ENV -Encoding utf8 -Append

          exit 0


      # ============================================================
      # 9. Build using ABE
      # ============================================================

      - name: Build with ABE
        if: ${{ inputs.action == 'Build' || inputs.action == 'Build and Deploy' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Starting ABE build"
          Write-Host "=================================================="

          $antExecutable = "$env:SAG_HOME/common/lib/ant/bin/ant.bat"

          $antBuildFile = `
              "$env:CICD_HOME/scripts/build-and-deploy/run/build.xml"

          Write-Host "SAG_HOME         = [$env:SAG_HOME]"
          Write-Host "SAG_CI_HOME      = [$env:CICD_HOME/scripts/build-and-deploy/setup]"
          Write-Host "ABE_SOURCE_DIR   = [$env:ABE_SOURCE_DIR]"
          Write-Host "targetEnv        = [$env:TARGET_ENVIRONMENT]"
          Write-Host "buildNumber      = [$env:MAIN_BUILDNUMBER]"
          Write-Host "projectName      = [$env:PROJECT_NAME]"
          Write-Host ""

          # Pass each -D option as an individual PowerShell argument.

          & $antExecutable `
              ("-DSAGHome={0}" -f $env:SAG_HOME) `
              ("-DSAG_CI_HOME={0}/scripts/build-and-deploy/setup" -f $env:CICD_HOME) `
              ("-Dbuild.source.dir={0}" -f $env:ABE_SOURCE_DIR) `
              ("-DtargetEnv={0}" -f $env:TARGET_ENVIRONMENT) `
              ("-DbuildNumber={0}" -f $env:MAIN_BUILDNUMBER) `
              ("-DprojectName={0}" -f $env:PROJECT_NAME) `
              -f "$antBuildFile" `
              build

          $antExitCode = $LASTEXITCODE

          Write-Host ""
          Write-Host "Ant exit code: [$antExitCode]"

          if ($antExitCode -ne 0) {
              Write-Error "webMethods ABE build failed."
              exit 1
          }

          Write-Host ""
          Write-Host "ABE build completed successfully."

          if ($env:CONFIG_DEPLOYMENT -ne "NONE") {

              $configurationZip = Join-Path $env:BUILD_OUTPUT_DIR "IS/isconfiguration.zip"
              $configurationAcDL = Join-Path $env:BUILD_OUTPUT_DIR "IS/isconfiguration.acdl"

              Write-Host ""
              Write-Host "Checking ABE configuration artifacts:"
              Write-Host "[$configurationZip]"
              Write-Host "[$configurationAcDL]"

              if (-not (Test-Path $configurationZip -PathType Leaf)) {
                  Write-Error "ABE did not create isconfiguration.zip: $configurationZip"
                  exit 1
              }

              if (-not (Test-Path $configurationAcDL -PathType Leaf)) {
                  Write-Error "ABE did not create isconfiguration.acdl: $configurationAcDL"
                  exit 1
              }

              Write-Host "Administrative configuration artifacts created successfully."
          }

          exit 0


      - name: Prepare Deployment Configuration
        if: ${{ inputs.action != 'Build' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Preparing deployment configuration"
          Write-Host "=================================================="


          Write-Host "Project Name           = [$env:PROJECT_NAME]"
          Write-Host "Target Environment     = [$env:TARGET_ENVIRONMENT]"
          Write-Host "Target IS Group Alias  = [$env:TARGET_ISGROUP_ALIAS]"
          Write-Host "Build Number           = [$env:MAIN_BUILDNUMBER]"
          Write-Host "Adapter Package        = [$env:HAS_ADAPTERPKG]"
          Write-Host "Adapter Config Package = [$env:ADAPTER_CONFIGPKG]"
          Write-Host "Execute WM Test        = [$env:EXECUTE_WMTEST]"


          if ($env:HAS_ADAPTERPKG -eq "true") {

              Write-Host ""
              Write-Host "Adapter package deployment requested."


              if ([string]::IsNullOrWhiteSpace($env:ADAPTER_CONFIGPKG)) {

                  Write-Error `
                      "Adapter configuration package is required when hasAdapterPackage is true."

                  exit 1
              }


              Write-Host `
                  "Adapter configuration package: [$env:ADAPTER_CONFIGPKG]"
          }
          else {

              Write-Host ""
              Write-Host "No Adapter package deployment requested."
          }


          exit 0


      # ============================================================
      # 11. Execute webMethods Test Suite
      # ============================================================

      - name: Execute webMethods Test Suite
        if: ${{ inputs.executeWmTestSuite == true && inputs.action != 'Build' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "webMethods Test Suite"
          Write-Host "=================================================="

          Write-Host "Test Suite execution requested."

          # Add WM Test Suite execution command here
          # when the test execution command is finalized.

          exit 0


      # ============================================================
      # 11. Deploy using Project Automator + Deployer
      # ============================================================

      - name: Deploy with Project Automator and Deployer
        if: ${{ inputs.action == 'Build and Deploy' || inputs.action == 'Deploy' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Starting webMethods deployment"
          Write-Host "=================================================="

          $antExecutable = "$env:SAG_HOME/common/lib/ant/bin/ant.bat"

          $antBuildFile = `
              "$env:CICD_HOME/scripts/build-and-deploy/run/build.xml"

          Write-Host ""
          Write-Host "Deployment parameters:"
          Write-Host "Action                = [$env:ACTION]"
          Write-Host "Build To Deploy       = [$env:BUILD_TO_DEPLOY]"
          Write-Host "Target Environment    = [$env:TARGET_ENVIRONMENT]"
          Write-Host "Target IS Group Alias = [$env:TARGET_ISGROUP_ALIAS]"
          Write-Host "Build Number          = [$env:MAIN_BUILDNUMBER]"
          Write-Host "Project Name          = [$env:PROJECT_NAME]"
          Write-Host "Previous Successful   = [$env:ROLLBACK_BUILD]"
          Write-Host "Build Backup Dir      = [$env:BUILD_BACKUP_DIR]"

          if (-not (Test-Path $antExecutable)) {
              Write-Error "Ant executable does not exist: $antExecutable"
              exit 1
          }

          if (-not (Test-Path $antBuildFile)) {
              Write-Error "CI/CD Ant build file does not exist: $antBuildFile"
              exit 1
          }

          if ($env:ACTION -eq "Deploy") {

              if ([string]::IsNullOrWhiteSpace($env:BUILD_TO_DEPLOY)) {
                  Write-Error "BUILD_TO_DEPLOY is required for Deploy."
                  exit 1
              }

              $buildPath = Join-Path $env:BUILD_BACKUP_DIR $env:BUILD_TO_DEPLOY

              Write-Host ""
              Write-Host "Existing build path:"
              Write-Host "[$buildPath]"

              if (-not (Test-Path $buildPath -PathType Container)) {
                  Write-Error "Requested build does not exist: $buildPath"
                  exit 1
              }
          }

          Write-Host ""
          Write-Host "=================================================="
          Write-Host "Executing Ant deployment"
          Write-Host "=================================================="

          if ($env:ACTION -eq "Deploy") {

              & $antExecutable `
                  ("-DSAGHome={0}" -f $env:SAG_HOME) `
                  ("-DSAG_CI_HOME={0}/scripts/build-and-deploy/setup" -f $env:CICD_HOME) `
                  ("-Daction={0}" -f $env:ACTION) `
                  ("-DbuildToDeploy={0}" -f $env:BUILD_TO_DEPLOY) `
                  ("-DbuildBackupDir={0}" -f $env:BUILD_BACKUP_DIR) `
                  ("-DtargetISGroupAlias={0}" -f $env:TARGET_ISGROUP_ALIAS) `
                  ("-DbuildNumber={0}" -f $env:MAIN_BUILDNUMBER) `
                  ("-DprojectName={0}" -f $env:PROJECT_NAME) `
                  -f "$antBuildFile" `
                  deploy

          }
          else {

              & $antExecutable `
                  ("-DSAGHome={0}" -f $env:SAG_HOME) `
                  ("-DSAG_CI_HOME={0}/scripts/build-and-deploy/setup" -f $env:CICD_HOME) `
                  ("-Daction={0}" -f $env:ACTION) `
                  ("-DSOURCE_DIR={0}" -f $env:BUILD_SOURCE_DIR) `
                  ("-DtargetEnv={0}" -f $env:TARGET_ENVIRONMENT) `
                  ("-DtargetISGroupAlias={0}" -f $env:TARGET_ISGROUP_ALIAS) `
                  ("-DbuildNumber={0}" -f $env:MAIN_BUILDNUMBER) `
                  ("-DprojectName={0}" -f $env:PROJECT_NAME) `
                  -f "$antBuildFile" `
                  deploy
          }

          $antExitCode = $LASTEXITCODE

          Write-Host ""
          Write-Host "Ant deployment exit code: [$antExitCode]"

          # ------------------------------------------------------------
          # Deployment succeeded
          # ------------------------------------------------------------

          if ($antExitCode -eq 0) {

              Write-Host ""
              Write-Host "=================================================="
              Write-Host "Deployment completed successfully."
              Write-Host "=================================================="

              # For Deploy Existing Build, the requested build is known.
              # For Build and Deploy, identify the newest ABE archive created
              # during this run. The deployment is serialized per environment
              # by the workflow concurrency group.
              if ($env:ACTION -eq "Deploy") {

                  $successfulBuild = $env:BUILD_TO_DEPLOY

              }
              else {

                  $candidateBuild = Get-ChildItem `
                      -Path $env:BUILD_BACKUP_DIR `
                      -Directory `
                      -Filter "build.*" |
                      Sort-Object LastWriteTime -Descending |
                      Select-Object -First 1

                  if ($null -eq $candidateBuild) {
                      Write-Error "Deployment succeeded, but no ABE build archive was found under $env:BUILD_BACKUP_DIR."
                      exit 1
                  }

                  $successfulBuild = $candidateBuild.Name
              }

              $stateDir = $env:DEPLOYMENT_STATE_DIR
              $stateFile = Join-Path $stateDir "$($env:TARGET_ENVIRONMENT).json"

              if (-not (Test-Path $stateDir)) {
                  New-Item -ItemType Directory -Path $stateDir -Force | Out-Null
              }

              $state = [ordered]@{
                  environment = $env:TARGET_ENVIRONMENT
                  projectName = $env:PROJECT_NAME
                  lastSuccessfulBuild = $successfulBuild
                  deploymentTimestamp = (Get-Date).ToString("o")
              }

              $state |
                  ConvertTo-Json -Depth 5 |
                  Set-Content -Path $stateFile -Encoding utf8

              Write-Host ""
              Write-Host "Deployment state updated:"
              Write-Host "Environment           : [$env:TARGET_ENVIRONMENT]"
              Write-Host "Last successful build : [$successfulBuild]"
              Write-Host "State file             : [$stateFile]"

              exit 0
          }

          # ------------------------------------------------------------
          # Deployment failed - attempt automatic rollback
          # ------------------------------------------------------------

          Write-Host ""
          Write-Host "=================================================="
          Write-Host "DEPLOYMENT FAILED"
          Write-Host "=================================================="
          Write-Host "Original deployment exit code: [$antExitCode]"

          if ([string]::IsNullOrWhiteSpace($env:ROLLBACK_BUILD)) {

              Write-Host ""
              Write-Host "No previous successful build is recorded for environment [$env:TARGET_ENVIRONMENT]."
              Write-Host "Automatic rollback cannot be performed."

              exit 1
          }

          $rollbackPath = Join-Path $env:BUILD_BACKUP_DIR $env:ROLLBACK_BUILD

          if (-not (Test-Path $rollbackPath -PathType Container)) {

              Write-Error "Rollback build does not exist: $rollbackPath"
              Write-Error "Deployment failed and rollback could not be started."
              exit 1
          }

          Write-Host ""
          Write-Host "=================================================="
          Write-Host "STARTING AUTOMATIC ROLLBACK"
          Write-Host "=================================================="
          Write-Host "Rollback build : [$env:ROLLBACK_BUILD]"
          Write-Host "Rollback path  : [$rollbackPath]"
          Write-Host ""

          # Reuse the existing Deploy target with the last known successful
          # build. This keeps rollback on the same Project Automator + Deployer
          # path already proven for Deploy Existing Build.
          & $antExecutable `
              ("-DSAGHome={0}" -f $env:SAG_HOME) `
              ("-DSAG_CI_HOME={0}/scripts/build-and-deploy/setup" -f $env:CICD_HOME) `
              "-Daction=Deploy" `
              ("-DbuildToDeploy={0}" -f $env:ROLLBACK_BUILD) `
              ("-DbuildBackupDir={0}" -f $env:BUILD_BACKUP_DIR) `
              ("-DtargetISGroupAlias={0}" -f $env:TARGET_ISGROUP_ALIAS) `
              ("-DbuildNumber={0}" -f $env:MAIN_BUILDNUMBER) `
              ("-DprojectName={0}" -f $env:PROJECT_NAME) `
              -f "$antBuildFile" `
              deploy

          $rollbackExitCode = $LASTEXITCODE

          Write-Host ""
          Write-Host "Rollback exit code: [$rollbackExitCode]"

          if ($rollbackExitCode -eq 0) {

              Write-Host ""
              Write-Host "=================================================="
              Write-Host "ROLLBACK COMPLETED SUCCESSFULLY"
              Write-Host "=================================================="
              Write-Host "Environment restored to: [$env:ROLLBACK_BUILD]"
              Write-Host "Original deployment: FAILED"
              Write-Host "Rollback: SUCCESS"

              # Keep the previous successful build as the state because the
              # failed build must never become the environment's known-good
              # version.
              exit 1
          }

          Write-Host ""
          Write-Host "=================================================="
          Write-Host "CRITICAL: ROLLBACK FAILED"
          Write-Host "=================================================="
          Write-Host "Original deployment: FAILED"
          Write-Host "Rollback: FAILED"
          Write-Host "Manual intervention is required."

          exit 1


      - name: Verify Generated Deployer Specification
        if: ${{ inputs.action != 'Build' }}
        shell: powershell
        run: |

          Write-Host "=================================================="
          Write-Host "Verifying generated Deployer specification"
          Write-Host "=================================================="


          $specFile = `
              "$env:CICD_HOME/scripts/build-and-deploy/run/ProjectAutomator/$env:PROJECT_NAME" +
              "DeploymentSpec.xml"


          Write-Host "Specification file:"
          Write-Host "[$specFile]"


          if (-not (Test-Path $specFile)) {

              Write-Error `
                  "Deployer specification was not found: $specFile"

              exit 1
          }


          Write-Host ""
          Write-Host "Target IS alias from generated specification:"


          Get-Content $specFile |
              Select-String -Pattern "<alias"


          $aliasLine = Get-Content $specFile |
              Select-String -Pattern '<alias type="IS">'


          if ($aliasLine -notmatch [regex]::Escape($env:TARGET_ISGROUP_ALIAS)) {

              Write-Error `
                  "Generated Deployer specification does not contain expected target IS Group Alias [$env:TARGET_ISGROUP_ALIAS]."

              exit 1
          }


          Write-Host ""
          Write-Host "Generated Deployer specification contains the expected target alias:"
          Write-Host "[$env:TARGET_ISGROUP_ALIAS]"

          exit 0

